import styled from 'styled-components';

export const HeaderCard = styled.section`
  padding: 3em;
  background: linear-gradient(
    90deg,
    var(--color-primary) 0%,
    var(--color-primary-contrast) 80%
  );
  border-radius: var(--border-radius);
`;
